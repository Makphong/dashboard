# Firebase / Firestore (server-side only)
# Option A (recommended): paste whole service account JSON as a one-line string
FIREBASE_SERVICE_ACCOUNT_JSON=

# Option B (split fields)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_PRIVATE_KEY_ID=
FIREBASE_CLIENT_ID=

# Optional: override local sqlite location for development
# LOCAL_DB_PATH=data/local_dashboard.db
